{% extends "base.html.twig" %}


{% block main %}
    <h1>The authors</h1>
    {% for aut in list_authors %}
    <p>
        <a href="{{ path('author_show', {'slug': aut.slug}) }}">
            {{ aut.firstName }} {{ aut.lastName }}
        </a>
    </p>
    {% endfor %}
{% endblock %}