<h4>The authors</h4>
<ul class="nav flex-column">
    {% for author in authors %}
    <li class="nav-item">
        <a class="nav-link" href="{{ path('author_show', {'slug': author.slug}) }}">
            {{ author.firstName }} {{ author.lastName }}
        </a>
    </li>
    {% endfor %}
</ul>