<h4>The categories</h4>
<ul class="nav flex-column">
    {% for category in categories %}
    <li class="nav-item">
        <a class="nav-link" href="{{ path('category_show', {'slug': category.slug}) }}">
            {{ category.title }}
        </a>
    </li>
    {% endfor %}
</ul>